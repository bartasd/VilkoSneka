const url = "http://localhost:3001";

export const dashboardUrl = `${url}/users`;
export const userCreateUrl = `${url}/users/create`;
export const authorizeUrl = `${url}/auth`;
export const userStatusUrl = `${url}/auth/status`;
export const messageCreateUrl = `${url}/message/create`;
export const getMessagesUrl = `${url}/message/getAll`;
